"use client";

import FeatureCarousel from "../ruixenui/feature-carousel";

export default function DemoFeatureCarousel() {
  return (
    <main className="bg-background flex flex-col items-center justify-center w-full">
      <FeatureCarousel />
    </main>
  );
}
