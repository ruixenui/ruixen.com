import { HeroTitleAnimation } from "@/registry/ruixenui/hero-title-animation";

export default function HeroTitleAnimationDemo() {
  return (
    <div className="relative w-full">
      <HeroTitleAnimation />
    </div>
  );
}
