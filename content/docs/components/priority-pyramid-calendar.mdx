---
title: Priority Pyramid Calendar
date: 2024-06-01
description: A pyramid-style calendar that organizes events by priority levels with visual hierarchy
author: ruixen
published: true
---

<ComponentPreview name="priority-pyramid-calendar-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ruixen.com/r/priority-pyramid-calendar"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="priority-pyramid-calendar" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Props

| Prop             | Type                        | Default | Description                 |
| ---------------- | --------------------------- | ------- | --------------------------- |
| `initialEvents`  | `Event[]`                   | `[]`    | Array of initial events     |
| `onEventsChange` | `(events: Event[]) => void` | -       | Callback when events change |

## Usage

```tsx
import {
  PriorityPyramidCalendar,
  Event,
} from "@/components/ui/priority-pyramid-calendar";

const events: Event[] = [
  {
    id: 1,
    title: "High Priority Task",
    date: new Date(),
    priority: 1,
  },
];

function MyCalendar() {
  const [events, setEvents] = React.useState<Event[]>([]);

  return (
    <PriorityPyramidCalendar
      initialEvents={events}
      onEventsChange={setEvents}
    />
  );
}
```
