---
title: Wave Background
date: 2024-06-01
description: A flowing wave background with WebGL shaders and theme support
author: ruixen
published: true
---

<ComponentPreview name="wave-background-demo" category="backgrounds" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ruixen.com/r/wave-background"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="wave-background" />

<Step>Update the import paths to match your project setup.</Step>

<Step>Install dependencies</Step>

```bash
npm install ogl
```

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import WaveBackground from "@/components/ruixen/wave-background";

export default function App() {
  return (
    <div className="relative h-screen w-full">
      <WaveBackground darkTheme={false} resolutionScale={1.0} />
      <div className="relative z-10">{/* Your content here */}</div>
    </div>
  );
}
```

## Props

| Prop              | Type      | Default | Description                           |
| ----------------- | --------- | ------- | ------------------------------------- |
| `darkTheme`       | `boolean` | `false` | Whether to use dark theme colors      |
| `resolutionScale` | `number`  | `1.0`   | Scale factor for rendering resolution |
