---
title: Calendar Twin
date: 2024-06-01
description: Dual-month range picker — click start, hover to preview, click end. Continuous band with smart rounding.
author: ruixen
published: true
---

<ComponentPreview name="calendar-twin-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ruixen.com/r/calendar-twin"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="calendar-twin" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Props

| Prop            | Type                                                   | Default | Description                         |
| --------------- | ------------------------------------------------------ | ------- | ----------------------------------- |
| `defaultStart`  | `string`                                               | -       | Initial range start in "YYYY-MM-DD" |
| `defaultEnd`    | `string`                                               | -       | Initial range end in "YYYY-MM-DD"   |
| `onRangeChange` | `(start: string \| null, end: string \| null) => void` | -       | Callback when the range changes     |
| `sound`         | `boolean`                                              | `true`  | Enable soft tick on interactions    |
