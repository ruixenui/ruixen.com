---
title: Auto Scrolling Client Carousel
date: 2024-08-24
description: Automatically scrolling carousel showcasing client logos with infinite loop
author: ruixen
published: true
---

<ComponentPreview name="auto-scrolling-client-carousel-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ruixen.com/r/auto-scrolling-client-carousel"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="auto-scrolling-client-carousel" />

<Step>Update the import paths to match your project setup.</Step>

<Step>Add the required CSS animations</Step>

```css title="app/globals.css" {1-8}
@keyframes scroll {
  from {
    transform: translateX(0);
  }
  to {
    transform: translateX(calc(-100% - var(--gap)));
  }
}
```

</Steps>

</TabsContent>

</Tabs>

## Props

| Prop        | Type       | Default                         | Description                         |
| ----------- | ---------- | ------------------------------- | ----------------------------------- |
| `title`     | `string`   | `"Trusted by industry leaders"` | Main heading text                   |
| `clients`   | `Client[]` | `defaultClients`                | Array of client objects             |
| `speed`     | `number`   | `20`                            | Animation speed in seconds          |
| `direction` | `string`   | `"left"`                        | Scroll direction: "left" or "right" |

## Client Interface

| Prop   | Type     | Description           |
| ------ | -------- | --------------------- |
| `name` | `string` | Client company name   |
| `logo` | `string` | Client logo image URL |

## Features

- Infinite auto-scrolling animation
- Customizable scroll speed and direction
- Responsive design with mobile optimization
- Pause on hover functionality
- Seamless loop without gaps
